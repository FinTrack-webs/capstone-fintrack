# --- Development (lokal) ---
HOST=localhost
PORT=3000

# --- Production (Render) — set di dashboard Render, JANGAN commit .env ---
# HOST=0.0.0.0
# PORT=(otomatis diisi Render, jangan set manual)
# NODE_ENV=production

# --- Database (Supabase PostgreSQL pooler) ---
DATABASE_USER=postgres.xxxxx
DATABASE_PASSWORD=your_supabase_password
DATABASE_HOST=aws-0-ap-southeast-1.pooler.supabase.com
DATABASE_PORT=6543
DATABASE_NAME=postgres

# --- Auth ---
# Production: gunakan secret baru yang panjang & acak (beda dari lokal)
JWT_SECRET=rahasia_jwt_fintrack_super_secret_key
